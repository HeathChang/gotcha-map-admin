NEXT_PUBLIC_API_BASE_URL=http://localhost:8060/api/v1
# 기본(운영 안전값)=false: 실제 백엔드 연동. 로컬에서 BE 없이 UI만 보려면 .env.local 에서 true.
# ⚠️ 운영 배포는 반드시 false + 실제 NEXT_PUBLIC_API_BASE_URL 을 build-time 에 주입.
NEXT_PUBLIC_USE_MOCK_API=false
